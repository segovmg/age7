fields:
  - name: id_pais
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: sqe_pais
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
      unique: true
  - name: nome
    type: string
    format: default
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_pais
