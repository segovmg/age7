fields:
  - name: id_especie
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: cd_especie
    type: string
    format: default
    constraints:
      required: true
  - name: nome
    type: string
    format: default
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_especie
