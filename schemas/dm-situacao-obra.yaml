fields:
  - name: id_situacao
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: cd_situacao
    type: string
    format: default
    constraints:
      required: true
  - name: nome
    type: string
    format: default
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_situacao
