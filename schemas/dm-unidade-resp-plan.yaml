fields:
  - name: id_unidade
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: ano_ppag
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: cd_unidade
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
  - name: nome
    type: string
    format: default
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_unidade
